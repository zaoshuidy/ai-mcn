# 决策日志（Decision Log）

> 记录关键方案决策。每个决策必须给出候选方向、评分、理由与风险。

| 决策编号 | 日期 | 所属阶段 | 问题 | 候选方向A | 候选方向B | 候选方向C | 各方向评分 | 最终选择 | 选择理由 | 风险 | 决策人 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-0001 | 2026-07-17 | Stage 0 | 项目总体架构路线 | A：全自研，零第三方组件 | B：重度依赖现成框架/Skill 拼装 | C：自研主流程＋精选第三方组件＋统一适配器 | A：可控性高但交付慢、重复造轮子（78）；B：起步快但可控性差、供应商锁定与安全风险高（72）；C：兼顾速度、可控与可替换（92） | C | 面试交付周期有限，需要平衡交付速度与工程质量；统一适配器保证任一第三方组件可替换，符合"可替换架构"与"最小权限"原则 | 第三方组件准入评审若不严格会引入安全与许可证风险，需靠 registry/ 机制强制把关 | 项目总控（依据阶段0任务书既定方针记录） |

| D-0002 | 2026-07-17 | Stage 0 返工 | 本地 master 与远程 main（含 Initial commit）历史无关联，如何交付 | A：强制推送覆盖远程 main | B：删除远程仓库重建 | C：本地更名 main 后以 --allow-unrelated-histories 合并远程历史再推送 | A：破坏远程历史，违反安全原则（40）；B：破坏性操作且需额外权限（30）；C：保留双方历史、可追溯、无强制操作（95） | C | 远程 Initial commit 是项目总控创建的真实历史，必须保留；合并不相关历史是标准做法；任务书明确禁止强制推送 | README add/add 冲突 1 处，已按"保留本地完整 README"原则解决并复核阶段标记 | 项目总控（任务书指定安全方式），技术执行 AI 执行 |

| D-0003 | 2026-07-17 | Stage 2 预研 | 达人采集与筛选工具选型（根据品牌Brief找出10位匹配达人） | A：xiaohongshu-mcp＋自研 xhs-benchmark-finder Skill | B：xhs-cli＋自研对标达人Skill | C：通用 Browser MCP＋网页操作Prompt | A：95（已封装小红书工具，输入输出稳定，MCP易接入）；B：92（自带SKILL.md、返回JSON，但速度较慢）；C：86（需AI自行识别页面结构，易受改版/风控影响） | A | 无成熟工具可直接完成全流程，采用"专用MCP采集公开数据＋自研Skill做关键词扩展、筛选、风格分析与评分"的混合方案；另登记 ibreez3/xiaohongshu-skill（89分）为方法参考、Browser MCP（86分）为兜底 | 采集工具需登录态，多处登录可能退出会话；平台风控与合规风险；须保留人工门禁，AI禁止点赞/评论/私信/批量抓取/绕过验证码 | 项目总控 |

| D-0004 | 2026-07-17 | Stage 1 | 品牌 Brief 结构化技术方案 | A：大模型自由 JSON 输出 | B：JSON Schema 校验 | C：Pydantic 模型＋JSON Schema＋业务规则校验 | A：72（业务适配18/25、数据稳定性8/20、工作流兼容9/15、可扩展7/10、可测试4/10、实现成本9/10、展示价值7/10；结构易漂移、无法机器校验）；B：84（业务适配20/25、稳定性16/20、兼容12/15、扩展7/10、可测试7/10、成本8/10、展示价值6/10；有结构校验但无 Python 类型层与业务规则层）；C：95（业务适配24/25、稳定性19/20、兼容14/15、扩展9/10、可测试10/10、成本8/10、展示价值9/10；类型安全＋机器可校验＋合规规则可执行） | C | 唯一达到 90 分门禁的方案；Pydantic 提供类型与默认值，JSON Schema 供跨语言/工作流消费，业务规则层承载合规边界（brand_claim/unverified、禁止臆造检测数据），且全部可用 pytest 离线测试 | 实现成本略高于 A/B；需新增 pydantic 依赖（轻量、主流、MIT 类许可证，属基础库非 Stage 2 候选组件） | 项目总控（任务书默认推荐 C，经三方向真实比较确认） |

| D-0005 | 2026-07-17 | Stage 1 局部返工 | Stage 1 验收 86/100 后的处理方式 | A：接受当前简化模型，直接进入 Stage 2 | B：删除现有实现并完全重写 | C：向后兼容扩展模型与门禁 | A：84（数据契约不足，Stage 2 无门禁依据）；B：88（质量可控但丢失 107 项测试，回归风险高）；C：96（保留全部测试、补齐下游契约、支持 90 分门禁、面试展示价值高） | C | 项目总控指定；保留已有 107 项测试避免回归，补齐标准子模型、18 项缺失信息、ValidationReport 评分与 Stage 2 自动放行机制 | 模型字段增多带来维护成本；用兼容函数 upgrade_legacy_brief 保证旧数据可读 | 项目总控 |

| D-0006 | 2026-07-17 | Stage 2 | 达人采集工具启用方式 | A：未完成审查，直接安装主工具并批量搜索 | B：全人工搜索，不接入任何自动化工具 | C：组件准入审查＋受控安装＋小规模POC＋人工门禁 | A：52（业务匹配16/20、数据能力14/15、稳定性4/10、风控2/15、许可证0/15、可替换2/10、可验证4/10、展示价值2/5；无审查直接违规）；B：71（业务匹配12/20、数据能力6/15、稳定性8/10、风控14/15、许可证15/15、可替换8/10、可验证6/10、展示价值2/5；安全但无法规模化且缺少工具治理展示）；C：94（业务匹配19/20、数据能力13/15、稳定性8/10、风控12/15、许可证13/15、可替换9/10、可验证9/10、展示价值5/5） | C | 唯一达到 90 分门禁；先静态审查再决定安装，POC 小规模受控，登录与互动操作保留人工门禁 | POC 依赖人工登录，进度受人工可用性影响；以 poc_required 状态如实记录，不伪造成功 | 项目总控（任务书默认方向 C，经三方向真实评分确认） |

| D-0007 | 2026-07-17 | Stage 2 | 4 个候选组件正式准入审查结论与候选采集路径 | A：放宽准入线批准 CAND-001（分数最高但无许可证） | B：全部 rejected 后暂停达人调研等待新工具 | C：4 组件全部 rejected，候选采集转人工执行＋保留重评通道 | A：35（无许可证属硬性排除，放行即违反准入规则）；B：68（合规但阻塞业务推进，人工路径本就可行）；C：93（严守准入规则、业务不停摆、上游改善后可重评） | C | 正式审查（证据 reports/component_reviews/evidence/，当日 GitHub 实时取证）显示：CAND-001 无 LICENSE 文件（74 分）、CAND-002 维护停滞（69 分）、CAND-003 依赖已拒绝上游（44 分）、CAND-004 明示规避验证码触发硬性排除（43 分），均低于 85 准入线；人工执行者按 creator_search_plan.json 与人工验证表执行候选采集，业务不被工具阻塞 | 人工采集进度慢于自动化；若后续出现合规只读组件需重新走完整准入流程（pending→under_review→poc_required→approved），不得跳级 | 项目总控（任务书准入规则），技术执行 AI 取证与记录 |

| D-0008 | 2026-07-18 | Stage 2 | 撤销"只能人工采集"结论，建立只读浏览器控制＋视频理解 POC 的技术路线 | A：坚持纯人工采集（D-0007 结论不变） | B：自研 Playwright CDP 连接用户 Chrome | C：kimi-webbridge 只读适配器＋策略层＋视频理解管线 | A：75（安全但无法验证自动化可行性，面试展示价值低）；B：82（需用户重启 Chrome 带调试端口，Chrome 136+ 限制默认 profile 的 CDP，操作门槛与失败率高；pip 安装已被项目总控中止）；C：94（现成第一方浏览器桥、复用真实登录态、零 Cookie 导出、守护进程本地可达已实测；写操作在适配器层白名单拦截） | C | 项目总控明确指令"浏览器控制直接用现成的 kimi-webbridge"；守护进程 http://127.0.0.1:10086 实测在线且用户已登录小红书；策略层（config/xhs_readonly_policy.yaml）将允许动作收敛为 navigate/snapshot/evaluate(内置只读脚本)/screenshot，click/fill/cdp 等一律拒绝 | 守护进程 navigate 对重页面有 30s load 超时，已用 location.href 软导航＋轮询就绪规避（实测有效）；yt-dlp 等 4 个 CLI 工具登记为 poc_required（CAND-005~008），GPL 工具仅 CLI 调用不复制源码 | 项目总控（指令），技术执行 AI 实现与取证 |

> 注：D-0003 为 Stage 2 预研候选登记（初评分）；D-0007 为正式准入审查结论（最终分），两者分离。CAND-001~004 经 2026-07-17 正式审查均为 rejected（registry/rejected_components.yaml），未安装任何组件，代码零引入；重评条件见各 rejected 记录的 re_evaluation_condition。
> D-0008 执行更新（2026-07-18 第二轮）：① 应项目总控要求彻底删除旧 navigate 入口——策略白名单移除 navigate（现仅 find_tab/list_tabs/snapshot/evaluate/screenshot），适配器三个高层方法统一为 search_notes_soft/open_profile_soft/open_note_soft 软导航（location.href＋轮询，复用已登录会话标签页，不创建新标签页），新增测试锁定；② 从已采集 5 位达人主页真实定位视频笔记：小娇日记747《爬坡后吃根香蕉 下颌线真的越来越清晰！！》（note_id 6a4903ad000000002003b221，type=video，page_observed），候选存 data/processed/xhs_video_candidate.json（canonical URL 不含 xsec_token）；③ 视频获取链真实执行：yt-dlp 2026.07.04 对未登录 explore 页面返回 No video formats found（未登录页面不含视频流），XHS-Downloader 需 Cookie 属策略禁止，唯一合规路径是从已登录页面提取 video_url——该路径被人工门禁阻塞（WebBridge 只能借用前台标签页，用户当前未查看小红书页面，守护进程 502 后已按规则重启一次并恢复）；④ 工具链全部就绪：FFmpeg v7.1（imageio-ffmpeg 0.6.0，CAND-009 登记 58 分 poc_required）、faster-whisper 1.2.1＋tiny 本地模型（CAND-010 登记 82 分 poc_required，经 hf-mirror/ModelScope 真实下载验证可加载）、12 字段 VideoSegment/证据清单/抽帧去重/全链路编排脚本完成，315 项测试全过；⑤ stage_2_video_poc_ready 保持 false，不伪造视频理解成功。
> 返工背景：Stage 0 首次远程交付缺失（成果仅在本地），项目总控验收评分 76/100，不允许进入 Stage 1，本次返工完成远程同步。
> 验收结论：Stage 0 已于远程交付复评中以 97/100 通过，允许进入 Stage 1。（完整记录：首次验收 76/100 未通过 → 远程返工 → 复评 97/100 通过。）

| D-0009 | 2026-07-18 | Stage 2 | WebBridge 前台借用协议性失败后的视频 POC 主链路 | A：继续 WebBridge 前台借用 | B：专用 Chrome 配置目录＋CDP 连接 | C：人工下载视频 | A：60（借用要求 OS 窗口焦点，用户回复对话即失焦，60+ 次尝试全部失败，协议性不可用）；B：96（专用 user-data-dir＋127.0.0.1 CDP，按 URL 选页不依赖焦点，Playwright Apache-2.0 已登记 CAND-011）；C：86（可靠但无法验证自动化链路，仅作最终降级） | B | 项目总控指令；专用 Profile 不读默认配置、不导出 Cookie、只读策略层拦截写操作；WebBridge 成果保留为搜索/截图备用 | 专用 Profile 需人工登录一次（600s 等待，超时诚实停止）；笔记直连 300031 需从主页提取公开 xsecToken | 项目总控（指令），技术执行 AI 实现 |
