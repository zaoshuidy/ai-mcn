# 高星开源项目参考说明

登记表含 26 个真实组件，其中 21 个星标不少于 1000。星标用于筛选参考信号，但不是直接依赖授权；许可证是硬门禁，参考不等于复制代码，运行依赖仍需单独准入。

primary 参考包括 nuwa-skill、humanizer、Humanizer-zh、langgraph、playwright、browser-use、video-use、MoneyPrinterTurbo、promptfoo、guardrails；FFmpeg 与 VideoCaptioner 仅以 CLI/subprocess 隔离使用，禁止复制源码，并记录 GPL/LGPL 边界。

`anthropics/skills` 虽星标高，但无可核验 LICENSE，登记为 `secondary_or_rejected` / `rejected_as_primary`；CAND-015/016 已降级。详见 `reports/high_star_reference_registry.md`、`reports/high_star_skill_mapping.md`。
