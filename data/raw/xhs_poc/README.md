# data/raw/xhs_poc/

本目录预留给小红书受控 POC 的原始返回数据。

**当前状态：空。**

原因：Stage 2 正式组件准入审查（2026-07-17）中，4 个候选组件全部 rejected
（详见 `reports/component_reviews/` 与 `registry/rejected_components.yaml`），
没有任何组件进入 POC 环节，因此不存在工具原始返回数据。

候选达人采集改由人工执行者按 `data/processed/creator_search_plan.json`
与 `reports/creator_human_review_template.md` 执行；人工采集的原始记录
（链接、截图编号、观察时间）可放入本目录，但严禁放入：

- Cookie / Session / Token / 二维码 / 登录账号信息；
- 非公开信息；
- 批量下载的视频文件。
